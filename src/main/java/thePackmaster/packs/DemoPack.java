/*
package thePackmaster.packs;

import basemod.AutoAdd;
import basemod.BaseMod;
import basemod.helpers.RelicType;
import com.megacrit.cardcrawl.cards.AbstractCard;
import com.megacrit.cardcrawl.core.CardCrawlGame;
import com.megacrit.cardcrawl.localization.UIStrings;
import com.megacrit.cardcrawl.unlock.UnlockTracker;
import thePackmaster.SpireAnniversary5Mod;
import thePackmaster.cards.AbstractPackmasterCard;
import thePackmaster.cards.demopack.*;
import thePackmaster.relics.AbstractPackmasterRelic;

import java.util.ArrayList;

import static thePackmaster.SpireAnniversary5Mod.modID;

public class DemoPack extends AbstractCardPack {
    public static final String ID = SpireAnniversary5Mod.makeID("DemoPack");
    private static final UIStrings UI_STRINGS = CardCrawlGame.languagePack.getUIString(ID);
    public static final String NAME = UI_STRINGS.TEXT[0];
    public static final String DESC = UI_STRINGS.TEXT[1];

    public DemoPack() {
        super(ID, NAME, DESC);
    }

    @Override
    public ArrayList<String> getCards() {
        ArrayList<String> cards = new ArrayList<>();
        cards.add(BurningPocket.ID);
        cards.add(FromMyHat.ID);
        cards.add(FullHouse.ID);
        cards.add(GrandSlam.ID);
        cards.add(GreedIsGood.ID);
        cards.add(GreedyPitch.ID);
        cards.add(HeavySlam.ID);
        cards.add(HitTheBooks.ID);
        cards.add(PocketLint.ID);
        cards.add(PreppingUp.ID);
        cards.add(RoadRage.ID);
        return cards;
    }
}


 */